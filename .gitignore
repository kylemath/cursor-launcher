# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
pip-log.txt
pip-delete-this-directory.txt

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.vsix

# Logs
*.log
logs/


# Files added by git-init-repo script
.vscode/settings.json
recent.json

# Cursor Project Launcher runtime
running.json
.visibility_cache.json
ports.json
github_cache.json
gh_assets/
pinned.json

# Full local identity — includes private repo names. The committed
# repos.json / repo_groups.json are public-only (written by --public).
*.local.json
repos.local.json
repo_groups.local.json

# Generated dashboard — contains private repo names, local paths, and
# embedded screenshots. Never commit this to the public repo.
dashboard.html

# App launcher / PWA build leftovers
build/
.app-launcher.log
*.app-launcher.log
.pwa-install-hinted
dev-dist/

# Sanitized build (generate_dashboard.py --public). Safe to share, but too
# large to track; use `git add -f dashboard_public.html` if you really want it.
dashboard_public.html
_site/
